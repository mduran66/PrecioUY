name: Build PrecioUY APK

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android 36
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Gradle 8.13
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      - name: Extract source
        run: |
          unzip -q PrecioUY_V1_source.zip -d buildsrc

      - name: Build APK
        working-directory: buildsrc/PrecioUY_V1
        run: gradle :app:assembleDebug --stacktrace

      - name: Prepare APK
        run: |
          cp buildsrc/PrecioUY_V1/app/build/outputs/apk/debug/app-debug.apk \
             PrecioUY-0.1.0-debug.apk

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: PrecioUY-APK
          path: PrecioUY-0.1.0-debug.apk

      - name: Create downloadable release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="v0.1.0-build-${GITHUB_RUN_NUMBER}"
          gh release create "$TAG" \
            PrecioUY-0.1.0-debug.apk \
            --title "Precio UY V0.1.0 - Build ${GITHUB_RUN_NUMBER}" \
            --notes "APK de prueba para Samsung/Android."
